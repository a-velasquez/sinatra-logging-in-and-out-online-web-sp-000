<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
    <h2>Hello,<%= Helpers.current_user(session).username %>, and welcome to your account</h2><br>
    <h2>Your current balance is <%= Helpers.current_user.balance %>.</h2><br>
    <a href='/logout'>Log Out</a>
    <% else %>
    <h2>Please<a href='/login'>Log in</a>to view your account</h2>
    <% end %>
  </body>
</html>
